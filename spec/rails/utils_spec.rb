# frozen_string_literal: true

RSpec.describe RailsUtils do
  it "has a version number" do
    expect(RailsUtils::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
